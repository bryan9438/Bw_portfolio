<!DOCTYPE html>
<html>
  <head>
    <title>BryanPortfolio</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <%= render "shared/navbar" %>

    <div class="background-banner d-flex justify-content-center align-items-center scroll-container">
      <div id="scroll-text" class="d-flex justify-content-center align-items-center bg-text">
        <%= image_tag "hand.png", class:"hand" %>
        <h1 class="text-center">Hello, I am Bryan</h1>
      </div>
    </div>

    <%= yield %>
  </body>
</html>
